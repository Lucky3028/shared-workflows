name: reviewdog
on:
  push:
    branches:
      - main
    paths:
      - ".github/**/*.yml"
  pull_request:
    paths:
      - ".github/**/*.yml"
jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: reviewdog/action-actionlint@v1
